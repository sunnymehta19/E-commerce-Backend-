<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>

    <style>
        /* remove number arrows */
        #mobileNumber::-webkit-inner-spin-button,
        #mobileNumber::-webkit-outer-spin-button {
            -webkit-appearance: none;
            margin: 0;
        }
        #mobileNumber {
            -moz-appearance: textfield;
        }
    </style>

    <title>Create Account</title>
</head>

<body class="bg-[#f1f1f1] min-h-screen flex justify-center items-center">

    <div class="flex flex-col items-center w-full">

        <!-- TITLE -->
        <div class="mb-7 text-2xl font-bold select-none text-center">
            Welcome to <span class="text-3xl text-amber-600">BagVerse</span>
        </div>

        <!-- CARD (Responsive width) -->
        <div class="
            bg-white rounded-xl px-5 py-4 shadow-md
            w-[85%] max-w-[280px]         <!-- Mobile size -->
            sm:max-w-[300px]             <!-- Small tablets -->
            md:max-w-[350px] md:w-[350px]<!-- Desktop unchanged -->
        ">

            <form action="/user/register" method="post">

                <div class="text-center text-xl font-bold mb-5 mt-1">
                    Create Account
                </div>

                <div class="flex flex-col">

                    <input class="bg-[#f1f1f1] outline-none rounded-lg px-3 py-2.5 mb-3 capitalize text-sm"
                        type="text" placeholder="Enter Full Name" name="username" required>

                    <input class="bg-[#f1f1f1] outline-none rounded-lg px-3 py-2.5 mb-3 text-sm"
                        type="number" placeholder="Enter Mobile Number +91" id="mobileNumber"
                        name="contact" required>

                    <input class="bg-[#f1f1f1] outline-none rounded-lg px-3 py-2.5 mb-3 capitalize text-sm"
                        type="text" placeholder="Enter Full Address" name="address" required>

                    <input class="bg-[#f1f1f1] outline-none rounded-lg px-3 py-2.5 mb-3 text-sm"
                        type="text" placeholder="Enter Email" name="email" required>

                    <input class="bg-[#f1f1f1] outline-none rounded-lg px-3 py-2.5 mb-4 text-sm"
                        type="password" placeholder="Enter Password" name="password" required>

                    <% if (error.length > 0) { %>
                    <div
                        class="flash-msg bg-red-100 border-l-4 border-red-500 text-red-800 
                               px-3 py-2 rounded-md shadow-md mb-3 text-xs text-center">
                        <%= error %>
                    </div>
                    <% } %>

                    <!-- SUBMIT BUTTON -->
                    <input
                        class="bg-amber-600 rounded-lg py-2.5 text-white font-bold text-base 
                               hover:bg-amber-700 transition cursor-pointer"
                        type="submit" value="Create">

                </div>

                <div class="flex justify-center gap-2 text-xs mt-3">
                    <p>Already have an account?</p>
                    <a class="text-blue-800 hover:underline" href="/login">Login</a>
                </div>

            </form>

        </div>

    </div>

</body>

</html>
