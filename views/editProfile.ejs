<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <link href="https://cdn.jsdelivr.net/npm/remixicon/fonts/remixicon.css" rel="stylesheet">
    <title>Edit Profile</title>
</head>

<body class="bg-gray-100 pt-[90px]">

    <%- include("partials/header") %>

    <div class="max-w-2xl mx-auto bg-white p-8 rounded-xl shadow-lg mt-10">

        <h2 class="text-2xl font-bold mb-6 text-gray-800">Edit Profile</h2>

        <form action="/edit-profile" method="POST" class="space-y-6">

            <!-- Username -->
            <div>
                <label class="block text-gray-600 font-medium mb-2">Username</label>
                <input 
                    type="text" 
                    name="username" 
                    value="<%= user.username %>"
                    class="w-full p-3 border rounded-lg shadow focus:ring-2 focus:ring-black outline-none" 
                    required />
            </div>

            <!-- Contact -->
            <div>
                <label class="block text-gray-600 font-medium mb-2">Contact Number</label>
                <input 
                    type="text" 
                    name="contact" 
                    value="<%= user.contact || '' %>"
                    class="w-full p-3 border rounded-lg shadow focus:ring-2 focus:ring-black outline-none"
                    placeholder="Enter your phone number" />
            </div>

            <!-- Email (read-only) -->
            <div>
                <label class="block text-gray-600 font-medium mb-2">Email (cannot be changed)</label>
                <input 
                    type="text" 
                    value="<%= user.email %>" 
                    class="w-full p-3 border rounded-lg bg-gray-100 text-gray-500 cursor-not-allowed" 
                    disabled />
            </div>

            <!-- Submit -->
            <button
                class="w-full bg-black text-white py-3 rounded-lg hover:bg-gray-800 transition-all">
                Save Profile
            </button>

        </form>

        <a href="/account" class="block text-center mt-4 text-gray-600 hover:underline">
            Cancel
        </a>
    </div>

</body>

</html>
